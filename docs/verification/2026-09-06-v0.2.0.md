# v0.2.0 发布候选验证

状态：代码检查通过；用户已接受当前内容，内容争议不再作为发布阻塞。尚未打 tag 或发布；真实三页整套实测范围仍按下文记录，不冒充已完成。

## 已验证

- `npm run verify:full`（样页复用补丁后重跑）：源码 52 通过、编译版 52 通过；各 1 个独立安装测试默认跳过。
- `npm run test:release-install`：独立打包安装通过，覆盖 3／12 页 CLI 完整流程。
- `npm run release:check -- --root <repo> --tag v0.2.0`：通过。
- 类型检查、构建、`git diff --check` 通过。
- 发布 PR 的首次 CI 暴露编译 CLI 在 Node 22.6 上误用 PPT 库未声明模块类型的 ESM 入口；改用库自带的 Node require 入口。回归显式关闭自动模块识别，修复前 3／12 页编译 CLI 均失败，修复后通过；不提高 Node 最低版本，也不改变生成内容。
- 整条发布分支只读 review 发现并修复一项 P1：转换器 13.333 英寸舍入导致拼页误拒。真实 converter donor（12191695×6857829 EMU）现可拼入 LAYOUT_WIDE，主文件尺寸与 donor 图形坐标保持原值。回归包含真正的尺寸／比例变化、缺失和非法尺寸仍拒绝。
- 依赖检查沿用两个不可达 image-size 漏洞例外，复查截止 2026-10-03。

## 样页复用补充验证

- 样页与正式页继续通过 `beginRequest` 附加真实用途与受众，不修改内容 prompt 或设计参数。
- TDD 回归先复现旧版未披露剩余预算、样页重复生图，再验证 3 页／12 页样页处于中间／末尾时按原位置复用。三页总请求数为 3，正式批次新增 2；未变且已检查的整套可零调用复用。
- 解包实际 fixture PPTX，按 slide relationship 定位样页图片并对照独特像素；原样页文件字节不变。中断重试、改用途／受众及显式重绘仍通过。
- 独立 review 发现并修复一项 P2：整套 QA 否决的代表页不能跨规划版本继续缓存。回归先失败、修复后通过；新样页生成并获批后才复用新产物。复核无剩余已发现问题。
- 技能基线与应用测试确认：只生成非缓存页，按新增调用数披露预算，缓存页仍进入整套 QA；未增加确认关卡。
- Mac 本地运行目录 `/Users/neomei/.local/share/superppt` 已同步 4 个实现／技能文件，逐字节与源码一致；3／12 页真实 CLI fixture 流程 2 项通过，保留原安装元数据并记录本地补丁。
- 修改前备份：`/Users/neomei/.local/share/superppt-sample-reuse-backup-2HQrSJ`。本补丁验证无真实模型调用、无新增费用、不修改既有医学任务。

## 真实生图与未完成项

- 新任务、水墨二档偏冷、脐疝科普样页；1 次宿主 OpenAI 实际调用正常返回，无 fallback、无重试。
- 实际 prompt 为 `beginRequest` 返回值，包含已批准原内容与真实用途说明；非画面用途说明没有被画为正文。
- 宿主 raw 与严格 16:9 master 均保留；master SHA-256：`f2a4e8aed77c265d41872a6ea07b2443967ab52d6248ccfb38b4693e574b686b`。
- 此前 Agent 对脐部图示及箭头含义提出疑虑，测试任务当时停在 sample-review，未继续三页整套。2026-09-06 用户明确反馈“内容没问题，不用纠结了”，接受当前内容；不再以该疑虑阻塞发布，也不为此追加改图或生图。本记录保留当时实际执行范围，不将用户接受内容写成独立医学准确性验证。
- 这证明本次出站调用可工作，不证明降低误拒率，也不证明医学图示准确性。
- 本轮没有 WPS GUI 编辑验收，也没有 Windows 宿主生图／Office GUI 验收。

下一步进入提交远端、CI 和 GitHub Release 流程；无需再次确认或修正本次内容。未执行的真实整套及 GUI 检查继续如实披露，不宣称通过。

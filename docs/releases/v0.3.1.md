# SuperPPT v0.3.1

本版修正安装包资源边界，确保风格预览图和代表图继续通过远程资源索引加载，不进入插件安装包。

## 主要变化

- npm 包显式排除 `skills/superppt/assets/styles/previews/` 和 `showcases/`，保留目录、来源和远程资源索引。
- 发布安装回归测试会注入图片探针，确认未来新增本地图片时也不会被打包。
- 本机安装已按新包更新，旧安装保留在独立备份目录。

## 验证

- 完整测试：114 项通过，0 项失败，1 项预期跳过。
- 类型检查、构建和可移植验证通过。
- 独立打包安装冒烟测试通过。
- 安装包不包含设计位图；选款页继续通过 `remote-assets.json` 加载公开 HTTPS 图片。

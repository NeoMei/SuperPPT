# SuperPPT 验收报告

日期：2026-08-27

## 离线验收

状态：PASS（仅限离线验收）。

本轮仅使用仓库内脱敏 Markdown、确定性 fake provider/reviewer 和 converter replay fixture。离线流程覆盖：内容导入、三阶段规划确认、单选风格样例、逐页生成与 QA、图片版组装、单页转换、真实 `replace-text` 编辑、预览确认、页面替换，以及 PPTX/PDF/蒙太奇/acceptance 同源哈希校验。

离线生成调用预算为 4 次：1 次代表页风格样例和 3 次正式页面生成；每个正式页面 1 次，低于每页 3 次上限。日志只记录 provider ID、页数、调用次数和临时项目相对路径，不写入提示词、源内容或凭证。

复验证据：

- focused E2E：1 PASS，0 FAIL。
- source 全量：224 total，222 PASS，2 个 Windows 专属用例在 macOS SKIP，0 FAIL。
- compiled 全量：224 total，222 PASS，2 个 Windows 专属用例在 macOS SKIP，0 FAIL。
- 类型检查、构建、仓库合同校验和 `git diff --check`：PASS。
- `scripts/acceptance-smoke.sh`：PASS；使用 bundled workspace runtime，成功后按默认策略清理独立临时目录。设置 `SUPERPPT_KEEP_ACCEPTANCE=1` 可保留成功运行的脱敏临时产物，失败运行始终保留现场并打印路径。

## 尚未完成的验收

- Live provider：PENDING。离线 PASS 不代表真实 provider 可用，也不构成真实模型输出质量证明；需要后续按 Gate 3 向用户展示 provider、页数、最大调用数、披露范围和输出路径并获得确认。
- Live editable conversion：PENDING。本轮使用经过生产校验器认证的 replay fixture，没有调用真实 OCR/vision，也没有使用真实 credentials。
- WPS/PowerPoint：PENDING。本轮未控制任何客户端；尚未完成打开、编辑、保存、关闭和重新打开的真实可编辑性交付验收。后续必须先通过 `npm run cli -- acceptance-smoke-copy --project <path>` 创建与本次 canonical PPTX 路径、项目 revision 和源 SHA 绑定的受控副本，只打开并保存返回的 `deck-smoke.pptx`，禁止打开或改写 canonical `deck.pptx`。完成客户端操作后，通过 `npm run cli -- acceptance-record --project <path> --input <file>` 提交 descriptor path、保存后副本 SHA、客户端、关闭/重开状态和观察结果；descriptor/source SHA 由项目内受信 anchor 提供，不能由客户端输入自证。

完成上述外部验收之前，`deliveryComplete` 必须保持 `false`，不得把离线文件生成或结构测试描述为整体交付完成。

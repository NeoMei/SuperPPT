# 决定与继续

默认只有 阶段契约.json 中三个决定。work 由 Agent 自动执行，不另加确认。

样页与整套的确认各自包含已展示的出站内容、参考图用途、调用预算和输出位置。
预算包括失败、fallback 与重试的实际请求；能力检查不算请求。
回执不明先找产物，不能把“不知道”变成“再付一次”。

用户主动修改时，手动保存关闭或 Agent 候选回看是该次改稿的确认，不增加默认制作路径的门。
完整 PPTX 确认交付后才返回 done。局部截图、文件存在或 fixture 测试不等于真实视觉验收。

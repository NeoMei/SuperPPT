interface:
  display_name: "SuperPPT"
  short_description: "Create image-first decks with three key decisions"
  default_prompt: "Use $superppt to turn my content into a reviewed PPTX with three key decisions and automatic machine steps."
policy:
  allow_implicit_invocation: true

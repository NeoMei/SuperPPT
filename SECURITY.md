# 文件与凭证边界

SuperPPT 是一次性本地 PPT 制作工具，不提供本地防篡改审计、HMAC 信任根、反回滚历史或旧项目迁移。

保留普通可靠性：任务内路径、短时写锁、原子状态更新、文件大小/格式校验、当前 PPTX 字节与页序检查。
手动保存文件直接采纳；不覆盖未知同名交付文件，不删除旧任务。

API key 不进入 prompt、仓库、日志或结果。需要的凭证由独立依赖通过其配置读取。
外部调用前展示出站文本、参考图用途、调用预算和输出位置；失败重试也计入预算。
宿主/API 路由由 ai-image-to-ppt 管理，安装元数据不代表实际可调用能力。

依赖漏洞检查和正式发布签名属于软件维护，与已删除的任务内部审计不同。
请勿在公开 issue 中提交密钥或客户内容。
